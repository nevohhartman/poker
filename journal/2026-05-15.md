Starting github learning journal


First importoted an image set for all the cards, and then created a simple struct for a card. I also had to create a quick 'encoder' I guess to make so that each suit corresponds to the right name of file.

Next displaying a pair of cards. This was so tedious and annoying; I ended up just using magic values (that is choosing values by hand) for spacing, as no matter how many calculations I did I coudln't figure out proper spacing.

Then shuffling: I got bored of doing visual so I quickly worked on shuffling and dealing. I ended up on a system (with the help of TA) of giving each card a "drawn" check, and then choosing a random card and checking if its alrady been drawn.