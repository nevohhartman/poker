Ok. I completed the round runner code, and so in theory, the game compeltely works now.The code wasnt too hard, though there was some ingenuity in figuring out when to stop betting, which was done by creating a count of active betters, and reducing it by one for each call, and resetting it - 1 with each raise. Overall, this was a lot of creating uniformity in the betting code, and unifying old and new code.


Now onto display. While the basic framework is there, the actual timing and order of code is completely messed up, as it was all done BEFORE ai.

Ok I redid the display constructor, and changed it to just load all bitmaps at the start, rather than loading at run.
I then added a redraw method to redraw the display in between ai turns. then a lot a lot of cleaning up(CLAUDE is very useful in this case). It caught for example that player turn was running my old structure, not including the last action field of player 0.


Now adding an "animation" for dealing and showing cards. will do this by just including a hole_cards array with 0 1 2 for each index corresponding to a player, and a "cards shown" check, so I can show cards sequentially.